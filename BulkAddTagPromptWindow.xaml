<Window x:Class="TodoApp.BulkAddTagPromptWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add Tag" Height="360" Width="380"
        Icon="Assets/icon.ico"
        WindowStyle="ToolWindow" ResizeMode="NoResize"
        ShowInTaskbar="False"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource BackgroundBrush}">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="PromptText" Grid.Row="0" FontSize="12" Foreground="{DynamicResource ForegroundBrush}" Margin="0,0,0,4" TextWrapping="Wrap"/>
        <TextBox x:Name="TagBox" Grid.Row="1" FontSize="13" Padding="5,4"
                 Foreground="{DynamicResource PrimaryTextBrush}"
                 Background="{DynamicResource BackgroundBrush}"
                 BorderBrush="{DynamicResource SecondaryBrush}"
                 TextChanged="TagBox_TextChanged"
                 KeyDown="TagBox_KeyDown"/>

        <!-- Existing tags only, filtered live as you type - mirrors TaskDetailViewModel's own
             FilteredAvailableTags list, just without a "+ Create" row since typing a name that
             doesn't match anything and pressing Add already creates it (see Add_Click). -->
        <ListBox x:Name="SuggestionsList" Grid.Row="2" Margin="0,8,0,0"
                 Background="{DynamicResource BackgroundBrush}"
                 Foreground="{DynamicResource PrimaryTextBrush}"
                 BorderBrush="{DynamicResource SecondaryBrush}"
                 MouseDoubleClick="Suggestion_DoubleClick"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Add" Width="70" Height="26" Margin="0,0,8,0" IsDefault="True" Click="Add_Click"/>
            <Button Content="Cancel" Width="70" Height="26" Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
