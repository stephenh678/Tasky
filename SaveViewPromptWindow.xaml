<Window x:Class="TodoApp.SaveViewPromptWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Save View" Height="170" Width="380"
        Icon="Assets/icon.ico"
        WindowStyle="ToolWindow" ResizeMode="NoResize"
        ShowInTaskbar="False"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource BackgroundBrush}">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Name:" FontSize="12" Foreground="{DynamicResource ForegroundBrush}" Margin="0,0,0,4"/>
        <TextBox x:Name="NameBox" Grid.Row="1" FontSize="13" Padding="5,4"
                 Foreground="{DynamicResource PrimaryTextBrush}"
                 Background="{DynamicResource BackgroundBrush}"
                 BorderBrush="{DynamicResource SecondaryBrush}"
                 KeyDown="InputBox_KeyDown"/>

        <TextBlock Grid.Row="2" Text="Enter to save · Esc to cancel" FontSize="10.5"
                   Foreground="{DynamicResource TertiaryBrush}" Margin="0,6,0,0"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,10,0,0">
            <Button Content="Save" Width="70" Height="26" Margin="0,0,8,0" IsDefault="True" Click="Save_Click"/>
            <Button Content="Cancel" Width="70" Height="26" Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
