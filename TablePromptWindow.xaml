<Window x:Class="TodoApp.TablePromptWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Insert Table" Height="195" Width="320"
        Icon="Assets/icon.ico"
        WindowStyle="ToolWindow" ResizeMode="NoResize"
        ShowInTaskbar="False"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource BackgroundBrush}">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="Columns:" FontSize="12" Foreground="{DynamicResource ForegroundBrush}" Margin="0,0,0,4"/>
        <TextBox x:Name="ColumnsBox" Grid.Row="1" Grid.Column="0" Text="3" FontSize="13" Padding="5,4" Margin="0,0,8,0"
                 Foreground="{DynamicResource PrimaryTextBrush}"
                 Background="{DynamicResource BackgroundBrush}"
                 BorderBrush="{DynamicResource SecondaryBrush}"
                 KeyDown="InputBox_KeyDown"/>

        <TextBlock Grid.Row="0" Grid.Column="1" Text="Rows:" FontSize="12" Foreground="{DynamicResource ForegroundBrush}" Margin="0,0,0,4"/>
        <TextBox x:Name="RowsBox" Grid.Row="1" Grid.Column="1" Text="3" FontSize="13" Padding="5,4"
                 Foreground="{DynamicResource PrimaryTextBrush}"
                 Background="{DynamicResource BackgroundBrush}"
                 BorderBrush="{DynamicResource SecondaryBrush}"
                 KeyDown="InputBox_KeyDown"/>

        <TextBlock Grid.Row="2" Grid.ColumnSpan="2" Text="Enter to insert · Esc to cancel" FontSize="10.5"
                   Foreground="{DynamicResource TertiaryBrush}" Margin="0,8,0,0"/>

        <StackPanel Grid.Row="3" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,10,0,0">
            <Button Content="Insert" Width="70" Height="26" Margin="0,0,8,0" IsDefault="True" Click="Insert_Click"/>
            <Button Content="Cancel" Width="70" Height="26" Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
