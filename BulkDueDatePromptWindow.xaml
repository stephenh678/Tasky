<Window x:Class="TodoApp.BulkDueDatePromptWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Set Due Date" Height="200" Width="380"
        Icon="Assets/icon.ico"
        WindowStyle="ToolWindow" ResizeMode="NoResize"
        ShowInTaskbar="False"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource BackgroundBrush}">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="PromptText" Grid.Row="0" FontSize="12" Foreground="{DynamicResource ForegroundBrush}" Margin="0,0,0,4" TextWrapping="Wrap"/>
        <DatePicker x:Name="DueDatePicker" Grid.Row="1" FontSize="13" HorizontalAlignment="Left" Width="150" KeyDown="InputBox_KeyDown"/>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,10,0,0">
            <Button Content="Clear Due Date" Width="110" Height="26" Margin="0,0,8,0" Click="Clear_Click"/>
            <Button Content="Set" Width="70" Height="26" Margin="0,0,8,0" IsDefault="True" Click="Set_Click"/>
            <Button Content="Cancel" Width="70" Height="26" Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
