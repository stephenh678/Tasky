<Window x:Class="TodoApp.LinkPromptWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Insert Link" Height="170" Width="380"
        Icon="Assets/icon.ico"
        WindowStyle="ToolWindow" ResizeMode="NoResize"
        ShowInTaskbar="False"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource BackgroundBrush}">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Row="0" Text="URL:" FontSize="12" Foreground="{DynamicResource ForegroundBrush}" Margin="0,0,0,4"/>
        <TextBox x:Name="UrlBox" Grid.Row="1" Text="https://" FontSize="13" Padding="5,4"
                 Foreground="{DynamicResource PrimaryTextBrush}"
                 Background="{DynamicResource BackgroundBrush}"
                 BorderBrush="{DynamicResource SecondaryBrush}"
                 KeyDown="InputBox_KeyDown"/>

        <TextBlock Grid.Row="2" Text="Enter to insert · Esc to cancel" FontSize="10.5"
                   Foreground="{DynamicResource TertiaryBrush}" Margin="0,6,0,0"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,10,0,0">
            <Button Content="Cancel" Width="70" Height="26" Margin="0,0,8,0" Click="Cancel_Click"/>
            <Button Content="Insert" Width="70" Height="26" IsDefault="True" Click="Insert_Click"/>
        </StackPanel>
    </Grid>
</Window>
